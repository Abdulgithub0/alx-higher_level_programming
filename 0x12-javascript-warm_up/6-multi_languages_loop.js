#!/usr/bin/node

/*
 * a script that prints 3 sentences by using an array of string and a loop
 */
const str = ['C is fun', 'Python is cool', 'JavaScript is amazing'];
for (let i = 0; i < str.length; i++) {
  console.log('%s', str[i]);
}
